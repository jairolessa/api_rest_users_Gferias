spring:
  application:
    name: api_users

  datasource:
    url: jdbc:postgresql://localhost:5436/gestaoferias
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false